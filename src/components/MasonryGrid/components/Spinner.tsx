import React from 'react';
import { LoadingSpinner } from '../styles';

/**
 * Loading spinner component displayed when loading more items
 */
const Spinner: React.FC = () => {
  return <LoadingSpinner />;
};

export default Spinner;